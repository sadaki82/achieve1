<div class="container">
  <div class="col-md-6 col-md-offset-3 col-sm-10 col-sm-offset-1">

<p><%= notice %></p>

<h2>コンタクト一覧</h2>

<% @contacts.each do |contact| %>
  <p>名前:<%= contact.name %></p>
  <p>メール:<%= contact.email %></p>
  <p>本文:<%= contact.content %></p>
<% end %>

<%= link_to "コンタクトを作成する", new_contact_path %>

  </div>
</div>